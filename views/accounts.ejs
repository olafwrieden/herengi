<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta http-equiv="X-UA-Compatible" content="ie=edge" />
    <title>Herengi - My Accounts</title>
    <link rel="stylesheet" href="style.css" />
    <link
      href="https://fonts.googleapis.com/css?family=Patrick+Hand&display=swap"
      rel="stylesheet"
    />
  </head>
  <body>
    <script type="text/javascript" src="script.js"></script>
    <header class="header">
      <h1 class="logo"><a href="#">Herengi</a></h1>
      <ul class="main-nav">
        <li><a href="#">Home</a></li>
        <li><a href="#">About</a></li>
        <li><a href="#">Portfolio</a></li>
        <li><a href="#">Contact</a></li>
      </ul>
    </header>
    <% Object.keys(accounts).forEach(function(account) { %>
    <div>
      <p>Select your account</p>
      <!-- Color the Account based on type (ie. transational or savings) -->
      <button
        onClick="window.location='/accounts/<%= accounts[account].accountId %>?accessToken=<%= accessToken %>'"
      >
        <%= accounts[account].displayName %>
      </button>
    </div>
    <% }); %>
  </body>
</html>
